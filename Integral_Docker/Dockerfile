# Используйте официальный образ с компилятором C++
FROM gcc:latest

# Создайте рабочую директорию внутри контейнера
WORKDIR /app

# Копируйте все файлы из текущего каталога (где находится Dockerfile) в контейнер
COPY . .

# Скомпилируйте C++ файл (замените "your-app.cpp" на фактическое имя вашего исходного файла)
RUN g++ -o Int Int.cpp

# Определите команду для запуска вашего приложения при старте контейнера
CMD ["./Int"]
